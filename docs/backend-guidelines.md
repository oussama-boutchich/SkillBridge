# Backend Guidelines

- Use PHP as API backend
- Separate config, routes, models, and services
- Validate all input data
- Protect role-based endpoints
- Keep responses consistent in JSON
- Log important admin actions
- Write readable and well-commented code
